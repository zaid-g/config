#source-file <path>

#  %% -------- [resource re-source] ----------:

# re-source resource
bind r source-file ~/.tmux.conf \; display-message "tmux config reloaded!"

#  %% -------- [time timeout time out refresh] ----------:

set -sg escape-time 0

# Set the status update interval (in seconds)
set-option -g status-interval 1

#  %% -------- [prefix] ----------:

# no timeout prefix
set-option -g prefix-timeout 0
unbind C-b
set-option -g prefix C-g
bind-key C-g send-prefix
# Bind 'i' to cancel command mode instead of showing pane info
bind i send-keys Escape


#  %% -------- [appearence shape UI looks organization color (copy these to configure on diff machine)] ----------:

set-option -g pane-border-lines double

set -g default-terminal "tmux-256color"

# Set the status bar style (background green, foreground black)
set-option -g status-style bg=green,fg=black

# Set the right side of the status bar
set -g status-right '#[fg=black,bg=green]%S'

# Change color when prefix is active
set -g status-left-length 100
set -g status-left '#{?client_prefix,#[bg=red]             ,#[bg=colour235]}'

# Set the color for the active window
set-window-option -g window-status-current-style fg=black,bg=blue,bold

# Set the color for inactive windows
set-window-option -g window-status-style fg=black,bg=green

# Optional: Set the pane border colors
set-option -g pane-border-style fg=green
set-option -g pane-active-border-style fg=blue,bg=#000000

# better copy mode color
set-window-option -g mode-style "fg=black,bg=blue"

#  %% -------- [copy mode stuff] ----------:

# history
set-option -g history-limit 250000

# vi mode
set-window-option -g mode-keys vi

unbind-key [
bind Escape copy-mode
bind -T copy-mode-vi i send -X cancel
# Unbind 'q' only in copy-mode-vi (so prefix+q in normal mode still works)
unbind-key -T copy-mode-vi q
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection
bind-key -T copy-mode-vi 'Y' send -X copy-selection-and-cancel

# clear history scroll
bind-key space clear-history \; display-message "History cleared!"
bind-key C-space clear-history \; display-message "History cleared!"

#  %% -------- [windows panes tabs] ----------:

bind X kill-window
bind c new-window -c "#{pane_current_path}" \; run-shell "bash ~/doc/config/tmux/rename_window.sh"
bind C new-window \; send-keys 'source ~/doc/config/tmux/new_project_window_random.zsh' Enter
bind C-c command-prompt -p "Name:" "new-window \; send-keys 'source ~/doc/config/tmux/new_project_window.zsh %1' Enter"
bind C-h previous-window
bind C-l next-window

# set tab name
bind-key R run-shell "bash $HOME/doc/config/tmux/rename_window.sh"

# renumber window index when closing
set-option -g renumber-windows on

# vim stuff window movements and splitting and resizing
bind-key s split-window -c "#{pane_current_path}"
bind-key v split-window -h -c "#{pane_current_path}"
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key -r M-h resize-pane -L 5
bind-key -r M-j resize-pane -D 5
bind-key -r M-k resize-pane -U 5
bind-key -r M-l resize-pane -R 5
# move pane to the far right/left/bottom/top
bind H splitw -fhb \; swapp -t ! \; killp -t !
bind L splitw -fh  \; swapp -t ! \; killp -t !
bind J splitw -fv  \; swapp -t ! \; killp -t !
bind K splitw -fvb \; swapp -t ! \; killp -t !

# switch windows
bind-key P swap-window -t -1\; select-window -t -1
bind-key N swap-window -t +1\; select-window -t +1

# switch to last tab
bind-key C-g last-window

# send pane to specific window position e.g. 1.0
bind-key M command-prompt -p "send pane to:"  "join-pane -t '%%'"
