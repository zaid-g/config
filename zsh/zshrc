# Instructions:
# - All variables declared inside functions must be local. WARN_CREATE_GLOBAL is set so will create noise for user if this instruction isn't followed.
# - All function and alias names are uppercase to avoid conflicts with system/installed tools. Shorthand declarations e.g. `CDCC` are meant to be invoked by user using keyboard so they are very short so user is quick. Long form definitions are for internal use only (called by other methods).
# - Each section `#  %% -------- [<section-name>] ----------:` contains configurations defined in the following order:
#     - long form function definitions
#     - shorthand function definitions.
#     - exports/settings 
#     - aliases 
# - Variable names and internal functions/aliases must be verbose and precise — named for exactly what they hold, not abbreviated. e.g. `file_count` not `n`, `downloads_file_path` not `source`, `prefix_to_add_to_file_name` not `prefix`.
# - Indent needs to be at 4 spaces (never 2)




#  %% -------- [machine setup/settings/options/configuration] ----------:

setopt WARN_CREATE_GLOBAL
setopt LOCAL_OPTIONS
setopt PROMPT_SUBST # expands PS1 single quote (for prompt)
export IS_DEV_MACHINE=false

# %% -------- [project creation and management brain parent] ----------:

function FIND_BRAIN_PARENT_PATH() {
    local folder_path="$PWD"
    while [[ "$folder_path" != "/" ]]; do
        if [[ -d "$folder_path/brain" ]]; then
            echo "$folder_path"
            return 0
        fi
        folder_path="$(dirname "$folder_path")"
    done
    echo "No 'brain' directory found searching up to filesystem root" >&2
    return 1
}

function FIND_BRAIN_PARENT_NAME() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    basename "$brain_parent_path"
}

# brain/junk = unorganized notes/pastes — "junk" means unfiled, NOT disposable
function TB() {
    mkdir -p brain/junk brain/logs brain/ai brain/command_output brain/tmp
    touch brain/notes.txt brain/junk/junk.txt brain/related.txt brain/ai/ai.txt brain/tags.txt brain/brag.txt
    [[ "$IS_DEV_MACHINE" == "true" ]] && TCH
}

# creates a brain parent and also sets it up as a git repository.
function MKBP() {
    MK "$@"
    GIG "$(basename "$PWD")" 2>/dev/null
    TB
    GI
    git add .
    git commit -m "init"
}

function TCH() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    mkdir -p "$brain_parent_path/brain"
    touch "$brain_parent_path/brain/command_history.txt"
    touch "$brain_parent_path/brain/command_history_full.txt"
}

function RCH() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    rm -f "$brain_parent_path/brain/command_history.txt"
    rm -f "$brain_parent_path/brain/command_history_full.txt"
}

function WB() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    echo "$brain_parent_path/brain"
}

function WCH() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    echo "$brain_parent_path/brain/command_history.txt"
}

function WN() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    echo "$brain_parent_path/brain/notes.txt"
}

function WJ() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    echo "$brain_parent_path/brain/junk/junk.txt"
}

function WAI() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    echo "$brain_parent_path/brain/ai/ai.txt"
}

function WR() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    echo "$brain_parent_path/brain/related.txt"
}

function CCH() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cat "$brain_parent_path/brain/command_history.txt"
}

function CN() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cat "$brain_parent_path/brain/notes.txt"
}

function CJ() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cat "$brain_parent_path/brain/junk/junk.txt"
}

function CAI() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cat "$brain_parent_path/brain/ai/ai.txt"
}

function CRE() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cat "$brain_parent_path/brain/related.txt"
}

function ARE() {
    if [[ $# -ne 1 ]]; then
        echo "Error: ARE requires exactly one argument (path)" >&2
        return 1
    fi
    
    if [[ ! -e "$1" ]]; then
        echo "Error: Path '$1' does not exist" >&2
        return 1
    fi
    
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    local full_file_path=$(readlink -f "$1")
    local temp_file_path=$(mktemp)
    
    # Add existing paths and new path to temp file, then sort unique
    if [[ -f "$brain_parent_path/brain/related.txt" ]]; then
        cat "$brain_parent_path/brain/related.txt" > "$temp_file_path"
    fi
    echo "$full_file_path" >> "$temp_file_path"
    
    # Remove duplicates and save back
    sort -u "$temp_file_path" > "$brain_parent_path/brain/related.txt"
    rm "$temp_file_path"
}

#  %% -------- [zsh hooks] ----------:

function ECHO_PREEXEC_COMMANDS() {
    echo $___COMMAND_TYPED
    echo $___COMMAND_ONELINE
    echo $___COMMAND_MULTILINE
}

function DEDUP_COMMAND_HISTORY() {
    local brain_parent_path=$(FIND_BRAIN_PARENT_PATH 2>/dev/null)
    local command_history_file_path="$brain_parent_path/brain/command_history.txt"
    [[ -z $brain_parent_path || ! -f $command_history_file_path || ! -s $command_history_file_path ]] && return
    awk '
        BEGIN { RS=""; ORS="\n\n" }
        { blocks[NR] = $0 }
        END { for (i=NR; i>=1; i--) print blocks[i] }
    ' "$command_history_file_path" | awk -v threshold="$___COMMAND_DURATION_LOGGING_THRESHOLD" '
        BEGIN { RS=""; ORS="\n\n" }
        {
            split($0, lines, "\n")
            split(lines[6], t, ":")
            secs = t[1]*3600 + t[2]*60 + t[3]
            if (secs > threshold) { print; next }
            key = lines[1] SUBSEP lines[2]
            if (!(key in seen)) { seen[key]; print }
        }
    ' | awk '
        BEGIN { RS=""; ORS="\n\n" }
        { blocks[NR] = $0 }
        END { for (i=NR; i>=1; i--) print blocks[i] }
    ' > "$command_history_file_path.tmp" && mv "$command_history_file_path.tmp" "$command_history_file_path"
}

function preexec() {
    # dedup command history
    DEDUP_COMMAND_HISTORY
    # flag to see if preexec ran
    ___PREEXEC_RAN_FOR_USER_ENTRY=true
    # what user inputted and from where
    ___COMMAND_TYPED="$1"
    ___COMMAND_ONELINE="$2"
    ___COMMAND_MULTILINE="$3"
    ___COMMAND_FORMATTED="${3//$'\r'/}"
    ___COMMAND_FORMATTED="${___COMMAND_FORMATTED//$'\n'/; }"
    ___COMMAND_PATH="$PWD"
    # datetime calculations
    ___COMMAND_START_EPOCH_SECONDS=$(date +%s)
    ___COMMAND_START_DATETIME=$(date '+%Y-%m-%d %H:%M:%S')
}

function precmd() {
    ___COMMAND_EXIT_CODE=$?
    # break if user entry didn't trigger preexec
    # e.g. user inputted enter without anything
    # or pure whitespace
    [[ -z $___PREEXEC_RAN_FOR_USER_ENTRY ]] && return
    ___PREEXEC_RAN_FOR_USER_ENTRY=""
    # compute timings
    ___COMMAND_END_EPOCH_SECONDS=$(date +%s)
    ___COMMAND_END_DATETIME=$(date '+%Y-%m-%d %H:%M:%S')
    ___COMMAND_ELAPSED=$(( ___COMMAND_END_EPOCH_SECONDS - ___COMMAND_START_EPOCH_SECONDS ))
    ___COMMAND_DURATION=$(printf '%02d:%02d:%02d' $((___COMMAND_ELAPSED/3600)) $((___COMMAND_ELAPSED%3600/60)) $((___COMMAND_ELAPSED%60)))
    # print execution time
    (( ___COMMAND_ELAPSED >= ___COMMAND_DURATION_LOGGING_THRESHOLD )) && echo "\n - Time: $___COMMAND_DURATION ($___COMMAND_START_DATETIME → $___COMMAND_END_DATETIME)\n"
    # log in command history file
    local brain_parent_path=$(FIND_BRAIN_PARENT_PATH 2>/dev/null)
    local command_history_file_path="$brain_parent_path/brain/command_history.txt"
    if [[ -n $brain_parent_path && -f $command_history_file_path && -n $___COMMAND_FORMATTED ]]; then
        printf '%s\n%s\nexit:%s\n%s\n%s\n%s\n\n' \
            "$___COMMAND_FORMATTED" "$___COMMAND_PATH" "$___COMMAND_EXIT_CODE" "$___COMMAND_START_DATETIME" "$___COMMAND_END_DATETIME" "$___COMMAND_DURATION" >> "$command_history_file_path"
    fi
    local command_history_full_file_path="$brain_parent_path/brain/command_history_full.txt"
    if [[ -n $brain_parent_path && -f $command_history_full_file_path && -n $___COMMAND_FORMATTED ]]; then
        printf '%s\n%s\nexit:%s\n%s\n%s\n%s\n\n' \
            "$___COMMAND_FORMATTED" "$___COMMAND_PATH" "$___COMMAND_EXIT_CODE" "$___COMMAND_START_DATETIME" "$___COMMAND_END_DATETIME" "$___COMMAND_DURATION" >> "$command_history_full_file_path"
    fi
}

function chpwd() {
    setopt NO_WARN_CREATE_GLOBAL
    local cwd_basename="${PWD:t}"
    [[ -z "$cwd_basename" ]] && return  # Skip if at root or empty
    local venv_path="$HOME/.virtualenvs/.$cwd_basename"
    if [[ -d "$venv_path" && "$VIRTUAL_ENV" != "$venv_path" ]]; then
        [[ -n "$VIRTUAL_ENV" ]] && deactivate
        # activate the associated venv. Keep it active even if leaving
        # to e.g. subdirectory (intentional)
        source "$venv_path/bin/activate"
    fi
    if [[ -n "$TMUX" ]] && [[ "$(tmux show-options -w -t $TMUX_PANE @AUTORENAME 2>/dev/null | grep -o '0$')" != "0" ]]; then
        TMUX_RENAME_WINDOW
    fi
}

# global var declaration so setopt WARN_CREATE_GLOBAL doesn't complain
export ___COMMAND_DURATION_LOGGING_THRESHOLD=5
___PREEXEC_RAN_FOR_USER_ENTRY=""
___COMMAND_TYPED=""
___COMMAND_ONELINE=""
___COMMAND_MULTILINE=""
___COMMAND_FORMATTED=""
___COMMAND_PATH=""
___COMMAND_START_EPOCH_SECONDS=""
___COMMAND_START_DATETIME=""
___COMMAND_END_EPOCH_SECONDS=""
___COMMAND_END_DATETIME=""
___COMMAND_ELAPSED=""
___COMMAND_DURATION=""
___COMMAND_EXIT_CODE=""

#  %% -------- [files folders path management] ----------:

function CLEAN_FILENAME() {
    echo -n "$1" | perl -CSD -pe 's/[\/:@%,=\s\x{00a0}\x{202f}\x{2007}\x{2009}\x{200a}\x{205f}\x{3000}*?\[\]|&;<>$`!"'"'"'(){}^~#\\]/-/g'
}

function PROCESS_DOWNLOAD_FILES_PATHS() {
    local prefix_to_add_to_file_name="" file_count=1 file_name downloads_file_path cleaned_filename destination_filename
    if [[ "$1" =~ ^[0-9]+$ ]] && [[ -n "$2" ]]; then
        file_count="$1"; prefix_to_add_to_file_name="$2"
    elif [[ "$1" =~ ^[0-9]+$ ]]; then
        file_count="$1"
    elif [[ -n "$1" ]]; then
        prefix_to_add_to_file_name="$1"
    fi
    local actual_file_count=$(ls ~/Downloads 2>/dev/null | wc -l | tr -d ' ')
    (( file_count > actual_file_count )) && file_count=$actual_file_count
    local file_names_in_downloads=$(ls -Art ~/Downloads 2>/dev/null | tail -n $file_count)
    [[ -z "$file_names_in_downloads" ]] && { echo "No files found" >&2; return 1; }
    echo "$file_names_in_downloads" | while IFS= read -r file_name; do
        downloads_file_path="$HOME/Downloads/$file_name"
        cleaned_filename=$(CLEAN_FILENAME "$file_name")
        destination_filename="${prefix_to_add_to_file_name:+${prefix_to_add_to_file_name}_}$cleaned_filename"
        echo "$downloads_file_path|$destination_filename"
    done
}

function GENERATE_PRONOUNCEABLE_STRING() {
    local string_target_length=${1:-8}
    local consonant_pool="bcdfghjklmnpqrstvwxyz"
    local vowel_pool="aeiou"
    local generated_string=""
    local i byte
    for ((i=0; i<string_target_length; i++)); do
        byte=$(od -An -tu1 -N1 /dev/urandom)
        if (( i % 2 == 0 )); then
            generated_string+=${consonant_pool:$((byte % ${#consonant_pool})):1}
        else
            generated_string+=${vowel_pool:$((byte % ${#vowel_pool})):1}
        fi
    done
    echo "$generated_string"
}

function CREATE_TIMESTAMPED_DIR_AND_CD() {
    local parent_folder_path="$1"
    shift
    local raw_folder_name="${*:-$(GENERATE_PRONOUNCEABLE_STRING 8)}"
    local cleaned_folder_name=$(CLEAN_FILENAME "$raw_folder_name")
    local timestamped_dir_path="${parent_folder_path:+$parent_folder_path/}${cleaned_folder_name}-$(date +%Y_%m_%d-%H_%M_%S)"
    mkdir -p "$timestamped_dir_path" && cd "$timestamped_dir_path"
}

function LLD() {
    local display_count=${1:-5}
    local total_file_count=$(ls ~/Downloads | wc -l | tr -d ' ')
    (( total_file_count > display_count )) && echo "..."
    ls -ltr --color=auto ~/Downloads/ | tail -n $display_count
}

#  %% --- [mkdir] ---:

function MKJ() {
    CREATE_TIMESTAMPED_DIR_AND_CD ~/doc/junk "$@"
}

function MK() {
    CREATE_TIMESTAMPED_DIR_AND_CD "" "$@"
}

function MKD() {
    CREATE_TIMESTAMPED_DIR_AND_CD ~/Downloads "$@"
}

#  %% --- [cd] ---:

function CDB() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cd "$brain_parent_path/brain"
}

function CDBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cd "$brain_parent_path"
}

function CDG() {
    local git_repo_root_path
    git_repo_root_path=$(git rev-parse --show-toplevel 2>/dev/null) || {
        echo "Not inside a git repository"
        return 1
    }
    cd "$git_repo_root_path"
}

#  %% --- [ls] ---:

function LLBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    LL "$brain_parent_path"
}

function LSBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    ls "$brain_parent_path"
}

#  %% --- [FP] ---:

function FPBP() {
    local brain_parent_path file_name
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    if [[ $# -eq 0 ]]; then
        readlink -f "$brain_parent_path"
    else
        for file_name in "$@"; do
            readlink -f "$brain_parent_path/$file_name"
        done
    fi
}

#  %% --- [rm] ---:

function RD() {
    local downloads_file_path destination_filename
    PROCESS_DOWNLOAD_FILES_PATHS "$@" | while IFS='|' read -r downloads_file_path destination_filename; do
        rm -rf "$downloads_file_path" && echo "Removed: $downloads_file_path"
    done
}

#  %% --- [mv] ---:

# mv folders from local Downloads to cwd in filesystem
function MVFD() {
    local downloads_file_path destination_filename
    PROCESS_DOWNLOAD_FILES_PATHS "$@" | while IFS='|' read -r downloads_file_path destination_filename; do
        mv "$downloads_file_path" "$destination_filename" && printf "\033[2m%s ->\033[0m\n\033[32m%s\033[0m\n" "$downloads_file_path" "$(readlink -f "$destination_filename")"
    done
    echo "\n\033[2mNext: $(PROCESS_DOWNLOAD_FILES_PATHS 1 | cut -d'|' -f1)\033[0m"
}

function MVFDTBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    local downloads_file_path destination_filename
    PROCESS_DOWNLOAD_FILES_PATHS "$@" | while IFS='|' read -r downloads_file_path destination_filename; do
        mv "$downloads_file_path" "$brain_parent_path/$(basename "$destination_filename")" && printf "\033[2m%s ->\033[0m\n\033[32m%s\033[0m\n" "$downloads_file_path" "$(readlink -f "$brain_parent_path/$(basename "$destination_filename")")"
    done
    echo "\n\033[2mNext: $(PROCESS_DOWNLOAD_FILES_PATHS 1 | cut -d'|' -f1)\033[0m"
}

# mv folders from local filesystem to local Downloads and touch timestamp
# so it appears at top of list
function MVTD() {
    local file_path
    [[ $# -eq 0 ]] && { echo "Usage: <command> <path>..."; return 1; }
    mkdir -p ~/Downloads
    for file_path in "$@"; do
        local resolved_path=$(readlink -f "$file_path")
        local cleaned_filename=$(CLEAN_FILENAME "$(basename "$resolved_path")")
        mv -v "$file_path" ~/Downloads/"$cleaned_filename"
        touch ~/Downloads/"$cleaned_filename"
    done
}

function MVTBP() {
    [[ $# -eq 0 ]] && { echo "Usage: <command> <path>..."; return 1; }
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    mv -v "$@" "$brain_parent_path/"
}

function MVDTBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    (cd "$brain_parent_path" && MVFD "$@")
}

# mv folders from local filesystem to trash
function MVTT() {
    local file_path
    [[ $# -eq 0 ]] && { echo "Usage: <command> <path>..."; return 1; }
    mkdir -p ~/trash
    for file_path in "$@"; do
        local resolved_path=$(readlink -f "$file_path")
        local cleaned_filename=$(CLEAN_FILENAME "$(basename "$resolved_path")")
        mv -v "$file_path" ~/trash/"$cleaned_filename"
    done
}

#  %% --- [cp] ---:

# cp folders from local Downloads to cwd in filesystem
function CPFD() {
    local downloads_file_path destination_filename
    PROCESS_DOWNLOAD_FILES_PATHS "$@" | while IFS='|' read -r downloads_file_path destination_filename; do
        cp "$downloads_file_path" "$destination_filename" && printf "\033[2m%s ->\033[0m\n\033[32m%s\033[0m\n" "$downloads_file_path" "$(readlink -f "$destination_filename")"
    done
}

# cp folders from local filesystem to local Downloads
function CPTD() {
    local file_path
    [[ $# -eq 0 ]] && { echo "Usage: <command> <path>..."; return 1; }
    mkdir -p ~/Downloads
    for file_path in "$@"; do
        local resolved_path=$(readlink -f "$file_path")
        local cleaned_filename=$(CLEAN_FILENAME "$(basename "$resolved_path")")
        cp -rv "$file_path" ~/Downloads/"$cleaned_filename"
    done
}

function CPTBP() {
    [[ $# -eq 0 ]] && { echo "Usage: <command> <path>..."; return 1; }
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    cp -rv "$@" "$brain_parent_path/"
}

function CPDTBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    (cd "$brain_parent_path" && CPFD "$@")
}

#  %% --- [tar] ---:

function TARGZ() {
    local target_path="${1:-.}"
    local full_file_path
    full_file_path="$(readlink -f "$target_path")"

    [[ ! -e "$full_file_path" ]] && { echo "Error: '$target_path' does not exist"; return 1; }

    local file_name
    file_name="$(basename "$full_file_path")"
    local tmp_output_file_path="$HOME/${file_name}.tar.gz"
    local final_output_file_path="$PWD/${file_name}.tar.gz"

    [[ -e "$final_output_file_path" ]] && { echo "Error: '$final_output_file_path' already exists"; return 1; }
    [[ -e "$tmp_output_file_path" ]] && { echo "Error: '$tmp_output_file_path' already exists"; return 1; }

    tar -czf "$tmp_output_file_path" -C "$(dirname "$full_file_path")" "$file_name" && mv "$tmp_output_file_path" "$final_output_file_path" && readlink -f "$final_output_file_path"
}

function UNTARGZ() {
    local archive_path="$1"
    [[ -z "$archive_path" ]] && { echo "Usage: UNTARGZ <archive.tar.gz>"; return 1; }
    [[ ! -f "$archive_path" ]] && { echo "Error: '$archive_path' does not exist or is not a file"; return 1; }
    local full_file_path
    full_file_path="$(readlink -f "$archive_path")"
    tar -xzf "$full_file_path" -C "$PWD"
}

alias CD="cd ~/doc"
alias CDD="cd ~/Downloads"
alias CDJ="cd ~/doc/junk"
alias CDT="cd ~/trash"
alias CDC="cd ~/doc/config"
alias CDCC="cd ~/doc/config/config"
# get full path
alias FP='readlink -f' # this IS available on OS X by default
# ls
alias LL="ls -l --color=auto"
alias LLA="ls -lA --color=auto"
alias LLR="ls -ltr --color=auto"
alias LLT="LLR ~/trash"
alias LSR="ls -tr --color=auto | tr '\n' '\0' | xargs -0 printf \"'%s'    \" && echo"

#  %% -------- [remote] ----------:

function SSH() {
    local forwarded_ports=()
    local forwarded_port
    for forwarded_port in "$@"; do
        [[ "$forwarded_port" =~ ^[0-9]+$ ]] || { echo "Usage: SSH [port]..."; return 1; }
        forwarded_ports+=("-L" "${forwarded_port}:localhost:${forwarded_port}")
    done
    ssh "${forwarded_ports[@]}" "$REMOTE"
}

# upload Downloads content to remote
# (from local Downloads to remote Downloads)
function UD() {
    local downloads_file_path destination_filename
    ssh "$REMOTE" "mkdir -p ~/Downloads"
    PROCESS_DOWNLOAD_FILES_PATHS "$@" | while IFS='|' read -r downloads_file_path destination_filename; do
        [ -e "$downloads_file_path" ] && rsync -avz "$downloads_file_path" "$REMOTE:~/Downloads/$destination_filename"
    done
}

# upload local filesystem content to remote Downloads
function U() {
    local file_path
    if [ $# -eq 0 ]; then
        echo "Usage: U <path>..."
        return 1
    fi
    ssh "$REMOTE" "mkdir -p ~/Downloads"
    local full_file_paths=()
    for file_path in "$@"; do
        full_file_paths+=($(readlink -f "$file_path"))
    done
    rsync -avz "${full_file_paths[@]}" "$REMOTE:~/Downloads/"
}

# download from remote (remote filesystem to local Downloads)
function D() {
    local remote_file_path
    [[ $# -eq 0 ]] && { echo "Usage: D <remote_file_path>..."; return 1; }
    [[ -z "$REMOTE" ]] && { echo "Error: REMOTE environment variable must be set"; return 1; }
    for remote_file_path in "$@"; do
        if rsync -rvz "$REMOTE:$remote_file_path" ~/Downloads/; then
            local downloaded_file_name=$(basename "$remote_file_path")
            chmod -R u+w ~/Downloads/${~downloaded_file_name}
            echo "✅ rsync succeeded for $REMOTE:$remote_file_path"
        else
            echo "❌ rsync failed for $REMOTE:$remote_file_path"
        fi
    done
}

# download from Remote's Downloads
# (remote Downloads to local Downloads)
function DD() {
    if [ -z "$REMOTE" ]; then
        echo "Error: REMOTE environment variable must be set"
        return 1
    fi
    local latest_file_in_remote_downloads=$(ssh "$REMOTE" "ls -Art ~/Downloads 2>/dev/null | tail -n 1")
    if [ -z "$latest_file_in_remote_downloads" ]; then
        echo "❌ No files found in remote Downloads"
        return 1
    fi
    if rsync -avz "$REMOTE:~/Downloads/$latest_file_in_remote_downloads" ~/Downloads/; then
        chmod -R u+w ~/Downloads/"$latest_file_in_remote_downloads"
        echo "✅ Downloaded to: ~/Downloads/$latest_file_in_remote_downloads"
    else
        echo "❌ rsync failed for $REMOTE:~/Downloads/$latest_file_in_remote_downloads"
        return 1
    fi
}

alias D='noglob D'

#  %% -------- [find search grep] ----------:

# Invariant: each search tool enters this section through exactly one door — find via _FIND,
# fd via _FD, rg via _RG (grep via GREP_CONTENTS_OF_FILE_PATHS_FROM_STDIN, fed by _FIND since
# grep cannot traverse). The door applies the house rules: gitignore exclusions, hidden files
# included, case-insensitive matching, 50MB content cap. Never invoke fd/find/rg/grep raw in
# a new function — call the door. (Using grep as a plain text filter on stdin, as the FILTER_*
# functions do on path strings, is not searching files and needs no door.)

# the reason we use the global gitignore is because we don't want to exclude via ripgrep's default
# behavior which skips locally declared paths in .gitignore. We have nested and gitignored repos
# that we want to include in search e.g. repo1/repo2/repo3 etc.
function GET_GITIGNORE_PATTERNS() {
    local gitignore_file="$HOME/doc/config/config/git/gitignore"
    if [[ ! -f "$gitignore_file" ]]; then
        echo "Error: gitignore file not found at $gitignore_file" >&2
        return 1
    fi
    local patterns=()
    local line
    while IFS= read -r line; do
        # Skip empty lines and comments
        [[ -z "$line" || "$line" == \#* ]] && continue
        # Strip trailing slashes (directory markers)
        line="${line%/}"
        patterns+=("$line")
    done < "$gitignore_file"
    printf '%s\n' "${patterns[@]}"
}

# each CONSTRUCT_* fills the array `constructed_exclude_args`, which the caller must declare local
function CONSTRUCT_FIND_EXCLUDE_ARGS_FROM_GITIGNORE_PATTERNS() {
    constructed_exclude_args=()
    local -a gitignore_patterns
    gitignore_patterns=("${(@f)$(GET_GITIGNORE_PATTERNS)}") || return 1
    local excluded_pattern
    for excluded_pattern in "${gitignore_patterns[@]}"; do
        constructed_exclude_args+=(-path "*/$excluded_pattern" -prune -o)
    done
}

function CONSTRUCT_FD_EXCLUDE_ARGS_FROM_GITIGNORE_PATTERNS() {
    constructed_exclude_args=()
    local -a gitignore_patterns
    gitignore_patterns=("${(@f)$(GET_GITIGNORE_PATTERNS)}") || return 1
    local excluded_pattern
    for excluded_pattern in "${gitignore_patterns[@]}"; do
        constructed_exclude_args+=(--exclude "$excluded_pattern")
    done
}

function CONSTRUCT_RG_EXCLUDE_ARGS_FROM_GITIGNORE_PATTERNS() {
    constructed_exclude_args=()
    local -a gitignore_patterns
    gitignore_patterns=("${(@f)$(GET_GITIGNORE_PATTERNS)}") || return 1
    local excluded_pattern
    for excluded_pattern in "${gitignore_patterns[@]}"; do
        constructed_exclude_args+=(--glob "!**/$excluded_pattern/**")
    done
}

# need this because `grep` doesn't have file traversal built in properly
# e.g. even grep -rni **/*.py <pattern>  <---- zsh is creating list here not
# grep via **/*.py
# multiple patterns are OR'd (repeated -e flags; BRE has no | alternation)
function GREP_CONTENTS_OF_FILE_PATHS_FROM_STDIN() {
    local pattern file_path
    local -a pattern_flags=()
    for pattern in "$@"; do
        pattern_flags+=(-e "$pattern")
    done
    local -a file_stat_result
    while IFS= read -r file_path; do
        zstat -A file_stat_result +size "$file_path" 2>/dev/null || continue
        [[ ${file_stat_result[1]} -gt $MAX_SEARCH_FILE_SIZE_BYTES ]] && continue
        grep -Hni "${pattern_flags[@]}" "$file_path"
    done
}

function FILTER_FILE_PATHS_FROM_STDIN_BY_EXTENSIONS() {
    if [ "$#" -eq 0 ]; then
        cat
    else
        local extensions_pattern=$(IFS='|'; echo "$*")
        grep -Ei -e "\.(${extensions_pattern})$"
    fi
}

function FILTER_FILE_PATHS_FROM_STDIN_BY_PATTERN() {
    if [ -z "$1" ]; then
        cat
    else
        grep -Ei -e "$1"
    fi
}

# lists brain parent paths sorted by their brain/.last_opened timestamp
function FD_BRAIN_PARENTS_SORT_BY_LAST_OPENED() {
    local last_opened_file_path
    local limit="${1:-0}"
    _FD \
        | FILTER_FILE_PATHS_FROM_STDIN_BY_PATTERN 'brain/\.last_opened$' \
        | while read -r last_opened_file_path; do
            echo "$(cat "$last_opened_file_path") $(dirname "$(dirname "$last_opened_file_path")")"
        done \
        | sort \
        | if [[ "$limit" -gt 0 ]]; then tail -n "$limit"; else cat; fi \
        | awk '{print $1 "          " $2}'
}

# Usage: _RG --output <lines|file_paths> --match <any|all> [--glob <path_glob>] [--ext <ext>]... -- <pattern>...
#   --output: what to print from the files that qualify
#     lines      - their matching lines, as ./path:line:content (for --match all: lines matching any of the patterns)
#     file_paths - their ./-prefixed paths
#   --match: which files qualify
#     any - files mentioning at least one of the patterns
#     all - files mentioning every pattern
#   --glob: only search files whose path matches this glob (default: everywhere)
#   --ext:  only search files with this extension (repeatable)
#   --  : end of flags; patterns follow (protects patterns that start with a dash)
# examples:
#   _RG --output lines --match any -- "foo"                                  - print lines that match foo, from all files
#   _RG --output lines --match any --ext py --ext js -- "foo"                - print lines that match foo, from .py/.js files only
#   _RG --output lines --match any --glob "**/brain/**" -- "foo"             - print lines that match foo, from brain files only
#   _RG --output lines --match any -- "foo" "bar"                            - print lines that match foo or bar
#   _RG --output lines --match all -- "foo" "bar"                            - print lines matching either foo or bar, from files that mention both
#   _RG --output lines --match all --ext py --ext txt -- "foo" "bar"         - same, but only from .py/.txt files
#   _RG --output file_paths --match any -- "foo" "bar"                       - print paths of files that mention foo or bar
#   _RG --output file_paths --match all -- "foo" "bar"                       - print paths of files that mention both foo and bar
#   _RG --output file_paths --match all --glob "**/brain/**" -- "foo" "bar"  - print paths of brain files that mention both
#   _RG --output lines --match any -- "-foo"                                 - dash-leading patterns are safe after --
function _RG() {
    local -a output_mode_opts match_mode_opts path_glob_opts extension_opts
    zparseopts -D -F -- -output:=output_mode_opts -match:=match_mode_opts -glob:=path_glob_opts -ext+:=extension_opts || return 1
    local output_mode="${output_mode_opts[2]}" match_mode="${match_mode_opts[2]}" scope_glob="${path_glob_opts[2]}"
    local -a file_extensions=("${(@)extension_opts:#--ext}")
    [[ "$output_mode" != "lines" && "$output_mode" != "file_paths" ]] && { echo "invalid _RG --output: $output_mode" >&2; return 1 }
    [[ "$match_mode" != "any" && "$match_mode" != "all" ]] && { echo "invalid _RG --match: $match_mode" >&2; return 1 }
    local file_extension
    if [[ "$match_mode" == "all" ]]; then
        local -a seed_flags=(--output file_paths --match any)
        [[ -n "$scope_glob" ]] && seed_flags+=(--glob "$scope_glob")
        for file_extension in "${file_extensions[@]}"; do
            seed_flags+=(--ext "$file_extension")
        done
        local surviving_file_paths=$(_RG "${seed_flags[@]}" -- "$1")
        local pattern
        for pattern in "${@[2,-1]}"; do
            [ -z "$surviving_file_paths" ] && return
            surviving_file_paths=$(echo "$surviving_file_paths" | tr '\n' '\0' | xargs -0 rg -l --ignore-case -e "$pattern")
        done
        [ -z "$surviving_file_paths" ] && return
        if [[ "$output_mode" == "file_paths" ]]; then
            echo "$surviving_file_paths"
        else
            echo "$surviving_file_paths" | tr '\n' '\0' | xargs -0 rg --with-filename --line-number --ignore-case -e "${(j:|:)@}"
        fi
        return
    fi
    local pattern="${(j:|:)@}"
    local -a output_mode_flags
    case "$output_mode" in
        lines) output_mode_flags=(--line-number) ;;
        file_paths) output_mode_flags=(--files-with-matches) ;;
    esac
    local extension_glob_flags=()
    for file_extension in "${file_extensions[@]}"; do
        extension_glob_flags+=("-g" "*.${file_extension}")
    done
    local -a scope_glob_flags=()
    [[ -n "$scope_glob" ]] && scope_glob_flags=(--glob "$scope_glob")
    local -a constructed_exclude_args
    CONSTRUCT_RG_EXCLUDE_ARGS_FROM_GITIGNORE_PATTERNS
    rg "${output_mode_flags[@]}" --ignore-case --no-ignore --hidden --glob-case-insensitive --max-filesize "$MAX_SEARCH_FILE_SIZE" "${scope_glob_flags[@]}" "${constructed_exclude_args[@]}" "${extension_glob_flags[@]}" -e "$pattern" .
}

function _FIND() {
    local -a constructed_exclude_args
    CONSTRUCT_FIND_EXCLUDE_ARGS_FROM_GITIGNORE_PATTERNS
    find . "${constructed_exclude_args[@]}" -type f -print
}

function _FD() {
    local -a constructed_exclude_args
    CONSTRUCT_FD_EXCLUDE_ARGS_FROM_GITIGNORE_PATTERNS
    fd --type f --no-ignore --hidden "${constructed_exclude_args[@]}" '.' .
}

function FIND() {
    echo
    _FIND | FILTER_FILE_PATHS_FROM_STDIN_BY_PATTERN "$1" | sort
}

function FINDT() {
    echo
    _FIND | FILTER_FILE_PATHS_FROM_STDIN_BY_EXTENSIONS "$@" | sort
}

function FD() {
    echo
    _FD | FILTER_FILE_PATHS_FROM_STDIN_BY_PATTERN "$1" | sort
}

function FDT() {
    echo
    _FD | FILTER_FILE_PATHS_FROM_STDIN_BY_EXTENSIONS "$@" | sort
}

function FDBP() {
    echo
    FD_BRAIN_PARENTS_SORT_BY_LAST_OPENED "$@"
}

# find files matching pattern one layer deep
function FIND1() {
    echo
    ls -d --color=always -1 *"$@"* | sort -t_ -k2,3
}

# Content-search vocabulary is a formula: RG [B] [ANY|ALL] — all args are patterns
#   B = brain files only; no suffix = print matching lines; ANY/ALL = print file paths
#   RG / RGB       -> lines matching any of the patterns (path:line:content)
#   RGANY / RGBANY -> file paths mentioning ANY of the patterns
#   RGALL / RGBALL -> file paths mentioning ALL of the patterns
# T variants filter by file type, like FINDT/FDT: last arg is the pattern, preceding args are extensions
#   RGT py js "foo" -> lines matching foo, in .py/.js files only
# GREP/GREPT = grep-engine siblings of RG/RGT for when rg isn't installed
function GREP() {
    echo
    _FIND | GREP_CONTENTS_OF_FILE_PATHS_FROM_STDIN "$@"
}

function GREPT() {
    echo
    _FIND | FILTER_FILE_PATHS_FROM_STDIN_BY_EXTENSIONS "${@[1,-2]}" | GREP_CONTENTS_OF_FILE_PATHS_FROM_STDIN "${@[-1]}"
}

function RG() {
    echo
    _RG --output lines --match any -- "$@"
}

function RGT() {
    echo
    local file_extension
    local -a extension_flags=()
    for file_extension in "${@[1,-2]}"; do
        extension_flags+=(--ext "$file_extension")
    done
    _RG --output lines --match any "${extension_flags[@]}" -- "${@[-1]}"
}

function RGB() {
    echo
    _RG --output lines --match any --glob "**/brain/**" -- "$@"
}

function RGANY() {
    echo
    _RG --output file_paths --match any -- "$@" | sort
}

function RGBANY() {
    echo
    _RG --output file_paths --match any --glob "**/brain/**" -- "$@" | sort
}

function RGALL() {
    echo
    _RG --output file_paths --match all -- "$@" | sort
}

function RGBALL() {
    echo
    _RG --output file_paths --match all --glob "**/brain/**" -- "$@" | sort
}

zmodload -F zsh/stat b:zstat # portable file-size checks (loads only the zstat builtin, system stat untouched)
MAX_SEARCH_FILE_SIZE_MEGABYTES=50
MAX_SEARCH_FILE_SIZE="${MAX_SEARCH_FILE_SIZE_MEGABYTES}M"                        # flag form for rg
MAX_SEARCH_FILE_SIZE_BYTES=$(( MAX_SEARCH_FILE_SIZE_MEGABYTES * 1024 * 1024 )) # byte form for stat-based checks

# %% -------- [vi vim lazyvim neovim nvim vi mode vi-mode] ----------:

# vim, VM taken
function V() {
    local file_path
    NVIM_APPNAME=$([[ "$IS_DEV_MACHINE" == "true" ]] && echo lazyvim || echo nvim) nvim "$@"
    for file_path in "${@:1:3}"; do readlink -f "$file_path"; done
}

function VBP() {
    local original_dir="$PWD"
    local file_name
    CDBP || return 1
    NVIM_APPNAME=$([[ "$IS_DEV_MACHINE" == "true" ]] && echo lazyvim || echo nvim) nvim "$@"
    for file_name in "$@"; do readlink -f "$file_name"; done
    cd "$original_dir"
}

function VB() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    local original_dir="$PWD"
    cd "$brain_parent_path"
    local file_paths=(brain/**/notes.txt brain/**/junk.txt brain/**/ai.txt brain/**/brag.txt)
    if [[ -f "$brain_parent_path/brain/command_history.txt" ]]; then
        file_paths+=(brain/command_history.txt)
        file_paths+=(brain/command_history_full.txt)
    fi
    local tab_count=${#file_paths[@]}
    file_paths+=(brain/**/*.txt)
    local nvim_brain_settings='
        vim.api.nvim_create_autocmd("CursorHold", {
            callback = function() vim.cmd("silent! wall") end
        })
        for _, buf in ipairs(vim.api.nvim_list_bufs()) do
            if vim.api.nvim_buf_get_name(buf):match("command_history%.txt$") or vim.api.nvim_buf_get_name(buf):match("command_history_full%.txt$") then
                vim.bo[buf].readonly = true
                vim.bo[buf].modifiable = false
                vim.bo[buf].autoread = true
                vim.api.nvim_create_autocmd({"CursorHold", "FocusGained", "BufEnter"}, {
                    buffer = buf,
                    command = "silent! checktime"
                })
            end
        end
    '
    NVIM_APPNAME=nvim nvim -p"$tab_count" \
        -c "lua $nvim_brain_settings" \
        "${file_paths[@]}"
    cd "$original_dir"
}

function VNOTES() {
    NVIM_APPNAME=nvim nvim \
        -c "lua vim.api.nvim_create_autocmd('CursorHold', {callback = function() vim.cmd('silent! wall') end})" \
        ~/doc/brain/notes.txt
}

function VJUNK() {
    NVIM_APPNAME=nvim nvim \
        -c "lua vim.api.nvim_create_autocmd('CursorHold', {callback = function() vim.cmd('silent! wall') end})" \
        ~/doc/brain/junk/junk.txt
}

function VAI() {
    NVIM_APPNAME=nvim nvim \
        -c "lua vim.api.nvim_create_autocmd('CursorHold', {callback = function() vim.cmd('silent! wall') end})" \
        ~/doc/brain/ai/ai.txt
}

function VP() {
    local file_paths
    if [ "$#" -eq 0 ]; then
        file_paths=($(FIND_NON_IGNORED_FILE_PATHS_MATCHING_TYPES))  # All allowed file_paths
    else
        file_paths=($(FIND_NON_IGNORED_FILE_PATHS_MATCHING_TYPES "$@"))  # Filtered by extensions
    fi
    if (( ${#file_paths[@]} )); then
        NVIM_APPNAME=$([[ "$IS_DEV_MACHINE" == "true" ]] && echo lazyvim || echo nvim) nvim "${file_paths[@]}"
    else
        echo "No matching file paths found outside forbidden folders."
    fi
}

function BEMOL() {
    bemol --verbose --watch
}

function VBEMOL() {
    bemol --verbose --watch > /tmp/bemol.log 2>&1 &
    local bemol_pid=$!
    NVIM_APPNAME=$([[ "$IS_DEV_MACHINE" == "true" ]] && echo lazyvim || echo nvim) nvim "$@"
    kill $bemol_pid 2>/dev/null
}

function VS(){
    local current_git_branch=$(git branch --show-current)
    NVIM_APPNAME=nvim nvim -S .vim/$current_git_branch.vim
}

function VZ() { NVIM_APPNAME=nvim nvim ~/.zshrc; }

function VT() { NVIM_APPNAME=nvim nvim ~/.tmux.conf; }

function VD() {
    local file_paths_in_downloads=($(PROCESS_DOWNLOAD_FILES_PATHS "$@" | cut -d'|' -f1))
    (( ${#file_paths_in_downloads[@]} )) && NVIM_APPNAME=$([[ "$IS_DEV_MACHINE" == "true" ]] && echo lazyvim || echo nvim) nvim "${file_paths_in_downloads[@]}"
}

# zsh vi mode
bindkey -v
export KEYTIMEOUT=1
# override this on dev machine
export NVIM_APPNAME="nvim" # lazyvim

# %% -------- [git] ----------:

function RESOLVE_GIT_BRANCH_OR_COMMIT() {
    local ref branch_name_that_matches commit_hash_that_matches
    read "ref?Diff against (enter for working tree): "
    [[ -z "$ref" ]] && { echo ""; return 0; }
    if git rev-parse --verify "$ref^{commit}" >/dev/null 2>&1; then
        echo "$ref"; return 0
    fi
    local branch_names_that_match=($(git branch --list "*$ref*" --format='%(refname:short)'))
    local commit_hashes_that_match=($(git log --all --oneline 2>/dev/null | awk '{print $1}' | grep "^$ref"))
    local options=()
    echo "No exact match. Options:" >&2
    local i=1
    for branch_name_that_matches in "${branch_names_that_match[@]}"; do
        echo "  $i) [branch] $branch_name_that_matches" >&2
        options+=("$branch_name_that_matches")
        ((i++))
    done
    for commit_hash_that_matches in "${commit_hashes_that_match[@]}"; do
        echo "  $i) [commit] $commit_hash_that_matches" >&2
        options+=("$commit_hash_that_matches")
        ((i++))
    done
    [[ ${#options[@]} -eq 0 ]] && { echo "No matches found" >&2; return 1; }
    local choice
    read "choice?Select: "
    [[ -z "$choice" || ! "$choice" =~ ^[0-9]+$ || $choice -lt 1 || $choice -gt ${#options[@]} ]] && { echo "Cancelled" >&2; return 1; }
    echo "${options[$choice]}"
}

function GET_GIT_LOG_COUNT() {
    local log_display_count=$GIT_LOG_COUNT
    local number_of_commits_ahead_of_remote
    if [[ -n "$1" ]]; then
        number_of_commits_ahead_of_remote=$(git -C "$1" rev-list --count origin/$(git -C "$1" rev-parse --abbrev-ref HEAD)..HEAD 2>/dev/null)
    else
        number_of_commits_ahead_of_remote=$(git rev-list --count origin/$(git rev-parse --abbrev-ref HEAD)..HEAD 2>/dev/null)
    fi
    (( number_of_commits_ahead_of_remote >= log_display_count )) && log_display_count=$((number_of_commits_ahead_of_remote + 1))
    echo $log_display_count
}

function GDBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    git -C "$brain_parent_path" diff
}

function GSBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    git -C "$brain_parent_path" status
}

function GACBP() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH) || return 1
    git -C "$brain_parent_path" add . && git -C "$brain_parent_path" commit -m 'F'
}

function GCH() {
    local branch branch_name_that_matches commit_hash_that_matches file_path
    if [[ $# -eq 0 ]]; then
        read "branch?Branch/Commit: "
        [[ -z "$branch" ]] && { echo "Branch required"; return 1; }
        [[ "$branch" == -* ]] && { echo "Invalid branch name"; return 1; }
        if git show-ref --verify --quiet "refs/heads/$branch"; then
            git checkout "$branch"
        elif git rev-parse --verify "$branch^{commit}" >/dev/null 2>&1; then
            git checkout "$branch"
        else
            local branch_names_that_match=($(git branch --list "*$branch*" --format='%(refname:short)'))
            local commit_hashes_that_match=($(git log --all --oneline 2>/dev/null | awk '{print $1}' | grep "^$branch"))
            echo "No exact match. Options:"
            local i=1
            local options=()
            for branch_name_that_matches in "${branch_names_that_match[@]}"; do
                echo "  $i) [branch] $branch_name_that_matches"
                options+=("branch:$branch_name_that_matches")
                ((i++))
            done
            for commit_hash_that_matches in "${commit_hashes_that_match[@]}"; do
                echo "  $i) [commit] $commit_hash_that_matches"
                options+=("commit:$commit_hash_that_matches")
                ((i++))
            done
            echo "  $i) [new branch] $branch"
            options+=("new:$branch")
            local choice
            read "choice?Select: "
            [[ -z "$choice" || ! "$choice" =~ ^[0-9]+$ || $choice -lt 1 || $choice -gt ${#options[@]} ]] && { echo "Cancelled"; return 1; }
            local selected_option="${options[$choice]}"
            case "$selected_option" in
                branch:*) git checkout "${selected_option#branch:}" ;;
                commit:*) git checkout "${selected_option#commit:}" ;;
                new:*)    git checkout -b "${selected_option#new:}" ;;
            esac
        fi
    else
        read "branch?Branch: "
        [[ -z "$branch" ]] && { echo "Branch required"; return 1; }
        [[ "$branch" == -* ]] && { echo "Invalid branch name"; return 1; }
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            if git -C "$file_path" show-ref --verify --quiet "refs/heads/$branch"; then
                git -C "$file_path" checkout "$branch"
            else
                git -C "$file_path" checkout -b "$branch"
            fi
        done
    fi
}

function GC() {
    local msg file_path
    read "msg?Commit message: "
    [[ -z "$msg" ]] && { echo "Message required"; return 1; }
    if [[ $# -eq 0 ]]; then
        git commit -m "$msg"
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" commit -m "$msg"
        done
    fi
}

function GB() {
    local file_path
    if [[ $# -eq 0 ]]; then
        git branch --sort=-committerdate
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" branch --sort=-committerdate
        done
    fi
}

function GPUSH() {
    local file_path
    if [[ $# -eq 0 ]]; then
        git push -u origin HEAD
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" push -u origin HEAD
        done
    fi
}

function GPUSHMAIN() {
    local file_path
    if [[ $# -eq 0 ]]; then
        git push -u origin main
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" push -u origin main
        done
    fi
}

function GPULL() {
    local file_path
    if [[ $# -eq 0 ]]; then
        local branch=$(git rev-parse --abbrev-ref HEAD)
        [[ "$branch" == "HEAD" ]] && { echo "Detached HEAD, cannot pull"; return 1; }
        git pull --rebase origin "$branch"
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            local branch=$(git -C "$file_path" rev-parse --abbrev-ref HEAD)
            [[ "$branch" == "HEAD" ]] && { echo "Detached HEAD, skipping"; continue; }
            git -C "$file_path" pull --rebase origin "$branch"
        done
    fi
}

function GPULLMAINLINE() {
    local file_path
    if [[ $# -eq 0 ]]; then
        [[ "$(git rev-parse --abbrev-ref HEAD)" == "HEAD" ]] && { echo "Detached HEAD, cannot pull"; return 1; }
        git pull --rebase origin mainline
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            [[ "$(git -C "$file_path" rev-parse --abbrev-ref HEAD)" == "HEAD" ]] && { echo "Detached HEAD, skipping"; continue; }
            git -C "$file_path" pull --rebase origin mainline
        done
    fi
}

function GPULLMAIN() {
    local file_path
    if [[ $# -eq 0 ]]; then
        [[ "$(git rev-parse --abbrev-ref HEAD)" == "HEAD" ]] && { echo "Detached HEAD, cannot pull"; return 1; }
        git pull --rebase origin main
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            [[ "$(git -C "$file_path" rev-parse --abbrev-ref HEAD)" == "HEAD" ]] && { echo "Detached HEAD, skipping"; continue; }
            git -C "$file_path" pull --rebase origin main
        done
    fi
}

function GD() {
    local ref file_path untracked_file_path
    if [[ $# -eq 0 ]]; then
        ref=$(RESOLVE_GIT_BRANCH_OR_COMMIT) || return 1
        {
            git --no-pager diff --color=always $ref
            git ls-files --others --exclude-standard | while read -r untracked_file_path; do
                git diff --color=always --no-index /dev/null "$untracked_file_path" 2>/dev/null
            done
        } 2>&1 | less -RXF
    else
        read "ref?Diff against (enter for working tree): "
        {
            for file_path in "$@"; do
                PRINT_AS_HEADER "$file_path"
                git -C "$file_path" --no-pager diff --color=always $ref
                git -C "$file_path" ls-files --others --exclude-standard | while read -r untracked_file_path; do
                    git -C "$file_path" diff --color=always --no-index /dev/null "$untracked_file_path" 2>/dev/null
                done
            done
        } 2>&1 | less -RXF
    fi
}

function GDNO() {
    local ref file_path
    if [[ $# -eq 0 ]]; then
        ref=$(RESOLVE_GIT_BRANCH_OR_COMMIT) || return 1
        git diff --name-only $ref
        git ls-files --others --exclude-standard
    else
        read "ref?Diff against (enter for working tree): "
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" diff --name-only $ref
            git -C "$file_path" ls-files --others --exclude-standard
        done
    fi
}

function GS() {
    local file_path
    {
        if [[ $# -eq 0 ]]; then
            git -c color.status=always status --untracked-files=all
        else
            for file_path in "$@"; do
                PRINT_AS_HEADER "$file_path"
                git -C "$file_path" -c color.status=always status --untracked-files=all
            done
        fi
    } 2>&1 | less -RXF
}

function GAMEND() {
    local msg file_path
    read "msg?Amend message: "
    [[ -z "$msg" ]] && { echo "Message required"; return 1; }
    if [[ $# -eq 0 ]]; then
        git commit --amend -m "$msg"
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" commit --amend -m "$msg"
        done
    fi
}

function GL() {
    local file_path
    {
        if [[ $# -eq 0 ]]; then
            git --no-pager log --color=always --graph --decorate --oneline -$(GET_GIT_LOG_COUNT)
        else
            for file_path in "$@"; do
                PRINT_AS_HEADER "$file_path"
                git -C "$file_path" --no-pager log --color=always --graph --decorate --oneline -$(GET_GIT_LOG_COUNT "$file_path")
            done
        fi
    } 2>&1 | less -RXF
}

function GI() {
    local file_path
    if [[ $# -eq 0 ]]; then
        if [[ ! -d .git ]]; then
            git init
        else
            echo "Git repository already exists"
        fi
        [[ ! -f .gitignore ]] && cp ~/doc/config/config/git/gitignore .gitignore
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" init
            [[ ! -f "$file_path/.gitignore" ]] && cp ~/doc/config/config/git/gitignore "$file_path/.gitignore"
        done
    fi
}

function GIG() {
    local pattern
    [[ -z "$1" ]] && { echo "Usage: GIG <pattern>"; return 1; }
    local git_repo_root_path
    git_repo_root_path=$(git rev-parse --show-toplevel 2>/dev/null) || { echo "Not a git repository"; return 1; }
    [[ ! -f "$git_repo_root_path/.gitignore" ]] && touch "$git_repo_root_path/.gitignore"
    [[ -s "$git_repo_root_path/.gitignore" && $(tail -c1 "$git_repo_root_path/.gitignore") != "" ]] && echo >> "$git_repo_root_path/.gitignore"
    for pattern in "$@"; do
        grep -Fxq "$pattern" "$git_repo_root_path/.gitignore" || echo "$pattern" >> "$git_repo_root_path/.gitignore"
    done
}
alias GIG='noglob GIG'

function GAC() {
    local msg file_path
    read "msg?Commit message (enter for timestamp): "
    [[ -z "$msg" ]] && msg="$(date '+%Y-%m-%d %H:%M:%S')"
    if [[ $# -eq 0 ]]; then
        local git_repo_root_path
        git_repo_root_path=$(git rev-parse --show-toplevel 2>/dev/null) || return 1
        git -C "$git_repo_root_path" add . && git -C "$git_repo_root_path" commit -m "$msg"
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            git -C "$file_path" add . && git -C "$file_path" commit -m "$msg"
        done
    fi
}

function GDIFF() {
    git rev-parse --git-dir >/dev/null 2>&1 || { echo "Not a git repo"; return 1; }
    local temp_file_path=$(mktemp) file_path
    git diff "$@" >> "$temp_file_path"
    if [[ $# -eq 0 ]]; then
        git ls-files --others --exclude-standard | while read -r file_path; do
            git diff --no-index /dev/null "$file_path" >> "$temp_file_path"
        done
    fi
    delta --side-by-side --line-numbers < "$temp_file_path"
    rm "$temp_file_path"
}

function GSQUASH() {
    if [[ $# -ne 2 ]]; then
        echo "Usage: GSQUASH <commit_hash> <commit_message>"
        return 1
    fi
    local commit_hash="$1"
    local commit_message="$2"
    if ! git rev-parse --verify "$commit_hash" >/dev/null 2>&1; then
        echo "Error: Invalid commit hash"
        return 1
    fi
    if ! git merge-base --is-ancestor "$commit_hash" HEAD; then
        echo "Error: $commit_hash is not an ancestor of HEAD"
        return 1
    fi
    git reset --soft "$commit_hash" && git commit -m "$commit_message"
}

# GSTASH creates a marker file before stashing to guarantee a stash entry
# even on clean repos. This prevents GSTASHPOP from accidentally popping
# an older unrelated stash when running across multiple packages.
function GSTASH() {
    local msg file_path
    read "msg?Stash message (enter for WIP): "
    [[ -z "$msg" ]] && msg="WIP stash"
    if [[ $# -eq 0 ]]; then
        local git_repo_root_path
        git_repo_root_path=$(git rev-parse --show-toplevel) || return 1
        touch "$git_repo_root_path/$GIT_STASH_FLAG_FILE" || { echo "Error: cannot create flag file"; return 1; }
        git stash push -u -m "$msg" || rm -f "$git_repo_root_path/$GIT_STASH_FLAG_FILE"
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            local git_repo_root_path
            git_repo_root_path=$(git -C "$file_path" rev-parse --show-toplevel) || continue
            touch "$git_repo_root_path/$GIT_STASH_FLAG_FILE" || { echo "Error: cannot create flag file"; continue; }
            git -C "$file_path" stash push -u -m "$msg" || rm -f "$git_repo_root_path/$GIT_STASH_FLAG_FILE"
        done
    fi
}

function GSTASHPOP() {
    local file_path
    if [[ $# -eq 0 ]]; then
        local git_repo_root_path
        git_repo_root_path=$(git rev-parse --show-toplevel) || return 1
        git stash pop -q && { echo "Popped"; rm -f "$git_repo_root_path/$GIT_STASH_FLAG_FILE"; }
    else
        for file_path in "$@"; do
            PRINT_AS_HEADER "$file_path"
            local git_repo_root_path
            git_repo_root_path=$(git -C "$file_path" rev-parse --show-toplevel) || continue
            git -C "$file_path" stash pop -q && { echo "Popped"; rm -f "$git_repo_root_path/$GIT_STASH_FLAG_FILE"; }
        done
    fi
}

export GIT_LOG_COUNT=4
export GIT_STASH_FLAG_FILE="_EMPTY_GIT_STASH_FLAG_FILE"
alias GR="git restore"
alias GA="git add"
alias GM="git merge"
alias GF="git fetch"
alias GFO="git fetch origin"

# %% -------- [python] ----------:

# install latest of these pkgs
function PIP3(){
    pip3 install ipdb
    pip3 install ipython
    pip3 install pandas
    pip3 install black
}

function PVENV(){
    local current_working_directory=$(pwd)
    local project_name="$(basename $(pwd))"
    deactivate
    if [ -d ~/.virtualenvs/.$project_name ]; then
        rm -rf ~/.virtualenvs/.$project_name
    fi
    mkdir -p .vim
    mkdir -p ~/.virtualenvs
    builtin cd ~/.virtualenvs
    if [ "$#" -eq 0 ]; then
        python3 -m venv ".$project_name"
    else
        $1 -m venv ".$project_name"
    fi
    . ./".$project_name"/bin/activate
    builtin cd $current_working_directory
    touch requirements.txt
    touch Dockerfile
    if ! [ -e .git ]; then
        git init
        echo "\n# vim\n.vim\n.ccls*" >> .gitignore
    fi;
    PIP3 # keep at end
}

alias I="python3 -m IPython"
alias P="python3"
alias PT="python3 -m pytest -sxvv"
alias PTDB="pytest -sxvv --pdb"
alias BLACK="python3 -m black **/*.py"
alias PC="pre-commit run -a"

# %% -------- [tmux] ----------:

function FIND_NESTED_TMUX_SERVER_SOCKET_FILE_PATHS() {
    local tmux_socket_dir="${TMUX_TMPDIR:-/tmp}/tmux-$(id -u)"
    print -l "$tmux_socket_dir"/nested_*(N)
}

function DISABLE_TMUX_WINDOW_AUTO_RENAME(){
    tmux set-window-option -t $TMUX_PANE @AUTORENAME 0
}

function ENABLE_TMUX_WINDOW_AUTO_RENAME(){
    tmux set-window-option -u -t $TMUX_PANE @AUTORENAME
}

function TMUX_SWAP_WINDOW_PREV() {
    local current_window_index=$(tmux display-message -p '#{window_index}')
    [[ "$current_window_index" -gt 0 ]] && tmux swap-window -t -1 \; select-window -t -1 || true
}

function TMUX_SWAP_WINDOW_NEXT() {
    local current_window_index=$(tmux display-message -p '#{window_index}')
    local last_window_index=$(tmux list-windows -F '#{window_index}' | tail -1)
    [[ "$current_window_index" -lt "$last_window_index" ]] && tmux swap-window -t +1 \; select-window -t +1 || true
}

function COMPUTE_TMUX_WINDOW_NAME_FROM_PWD() {
    [[ "$PWD" == "/" ]] && return
    local window_name
    window_name=$(FIND_BRAIN_PARENT_NAME 2>/dev/null) || window_name=$(basename "$PWD")
    [[ ${#window_name} -gt 15 ]] && window_name="${window_name:0:12}…"
    echo "$window_name"
}

function TMUX_RENAME_WINDOW() {
    local window_name
    window_name=$(COMPUTE_TMUX_WINDOW_NAME_FROM_PWD)
    [[ -z "$window_name" ]] && return
    tmux rename-window -t "$TMUX_PANE" "$window_name"
}

function TAS() {
    if [[ -n "$1" ]]; then
        tmux attach-session -t "$1"
        return
    fi
    local tmux_session_count=$(tmux list-sessions 2>/dev/null | wc -l | tr -d ' ')
    if [[ "$tmux_session_count" -eq 1 ]]; then
        tmux attach-session
    elif [[ "$tmux_session_count" -gt 1 ]]; then
        echo "Specify session:"
        tmux list-sessions
        return 1
    else
        echo "No tmux sessions"
    fi
}

function TKS() {
    if [[ -n "$1" ]]; then
        tmux kill-session -t "$1"
        return
    fi
    local tmux_session_count=$(tmux list-sessions 2>/dev/null | wc -l | tr -d ' ')
    if [[ "$tmux_session_count" -eq 1 ]]; then
        tmux kill-session
    elif [[ "$tmux_session_count" -gt 1 ]]; then
        echo "Specify session:"
        tmux list-sessions
        return 1
    else
        echo "No tmux sessions"
    fi
}

# Tmux open defaults
function TOD() {
    CD # guard, shouldn't do anything
    local target_pane_id="$TMUX_PANE"
    tmux send-keys -t "$target_pane_id" 'TOT' Enter
    target_pane_id=$(tmux new-window -P -F '#{pane_id}')
    tmux send-keys -t "$target_pane_id" 'CD && TOBP' Enter
    target_pane_id=$(tmux new-window -P -F '#{pane_id}')
    tmux send-keys -t "$target_pane_id" 'CDC && TOBP' Enter
}

# tmux open terminal window
function TOT() {
    local target_pane_id="$TMUX_PANE"
    tmux send-keys -t "$target_pane_id" 'CDJ' Enter
    tmux send-keys -t "$target_pane_id" 'DISABLE_TMUX_WINDOW_AUTO_RENAME' Enter
    tmux send-keys -t "$target_pane_id" 'tmux rename-window -t $TMUX_PANE term' Enter
}

# tmux open brain parent
function TOBP() {
    if [[ $# -eq 0 ]]; then
        CDBP || return 1
        date +%Y_%m_%d-%H_%M_%S > brain/.last_opened
        local left_pane_id="$TMUX_PANE"
        # Nested tmux sessions with auto-cleanup
        tmux send-keys -t "$left_pane_id" 'T' Enter
        tmux send-keys -t "$left_pane_id" 'AIR' Enter
        tmux send-keys -t "$left_pane_id" C-g n C-g n C-g 0
        local right_pane_id=$(tmux split-window -h -t "$left_pane_id" -P -F '#{pane_id}')
        tmux send-keys -t "$right_pane_id" 'T' Enter
        tmux send-keys -t "$right_pane_id" 'VB' Enter
        tmux send-keys -t "$right_pane_id" C-g n C-g n C-g 0
        local bottom_right_pane_id=$(tmux split-window -v -t "$right_pane_id" -P -F '#{pane_id}')
        tmux send-keys -t "$bottom_right_pane_id" 'DISABLE_TMUX_WINDOW_AUTO_RENAME' Enter
        tmux select-pane -t "$right_pane_id"
    else
        local dir
        for dir in "${(Oa)@}"; do
            local pane_id=$(tmux new-window -a -d -c "$dir" -P -F '#{pane_id}')
            tmux send-keys -t "$pane_id" 'TOBP' Enter
        done
    fi
}

# tmux create and open brain parent
function TCOBP() {
    local target_pane_id
    target_pane_id=$(tmux new-window -a -c "$PWD" -P -F '#{pane_id}')
    tmux send-keys -t "$target_pane_id" "MKBP $*" Enter
    tmux send-keys -t "$target_pane_id" "TOBP" Enter
}

function TCOBPJ() {
    local target_pane_id
    target_pane_id=$(tmux new-window -c ~/doc/junk -P -F '#{pane_id}')
    tmux send-keys -t "$target_pane_id" "MKBP $*" Enter
    tmux send-keys -t "$target_pane_id" "TOBP" Enter
}

function TO3() {
    local target_pane_id="$TMUX_PANE"
    tmux split-window -h -t "$target_pane_id"
    tmux split-window -v -t "$target_pane_id"
    tmux select-pane -t "$target_pane_id"
}

function T() {
    if [[ -n "$TMUX" ]]; then
        local nested_tmux_server_socket_name="nested_$(date +%Y_%m_%d-%H_%M_%S)_$RANDOM"
        # name the window at creation: a fresh shell never fires chpwd, so auto-rename wouldn't
        local window_name=$(COMPUTE_TMUX_WINDOW_NAME_FROM_PWD)
        local -a window_name_args=()
        [[ -n "$window_name" ]] && window_name_args=(-n "$window_name")
        TMUX= COLOR_THEME=$COLOR_THEME COLOR_TEXT_IF_COLORED_BACKGROUND=$COLOR_TEXT_IF_COLORED_BACKGROUND COLOR_HIGHLIGHT_TEXT_TMUX=$COLOR_HIGHLIGHT_TEXT_TMUX COLOR_ACTIVE=$COLOR_ACTIVE COLOR_HEX_ACTIVE_BACKGROUND=$COLOR_HEX_ACTIVE_BACKGROUND tmux -2 -L "$nested_tmux_server_socket_name" new-session "${window_name_args[@]}" "$@" \; set destroy-unattached on
    else
        COLOR_THEME=$COLOR_THEME COLOR_TEXT_IF_COLORED_BACKGROUND=$COLOR_TEXT_IF_COLORED_BACKGROUND COLOR_HIGHLIGHT_TEXT_TMUX=$COLOR_HIGHLIGHT_TEXT_TMUX COLOR_ACTIVE=$COLOR_ACTIVE COLOR_HEX_ACTIVE_BACKGROUND=$COLOR_HEX_ACTIVE_BACKGROUND tmux -2 "$@"
    fi
}

# -L default is explicit because bare tmux commands run from inside a nested pane would
# resolve to that pane's nested server via $TMUX
function TLS() {
    echo "default:"
    tmux -L default ls 2>/dev/null || echo "  (no server)"
    local nested_tmux_server_socket_file_path server_listing
    for nested_tmux_server_socket_file_path in $(FIND_NESTED_TMUX_SERVER_SOCKET_FILE_PATHS); do
        if server_listing=$(tmux -L "${nested_tmux_server_socket_file_path:t}" ls 2>/dev/null); then
            echo "${nested_tmux_server_socket_file_path:t}:"
            echo "$server_listing"
        else
            # dead server leaves its socket file behind — clean it up silently
            rm -f "$nested_tmux_server_socket_file_path"
        fi
    done
}

function TKAS() {
    local nested_tmux_server_socket_file_path
    for nested_tmux_server_socket_file_path in $(FIND_NESTED_TMUX_SERVER_SOCKET_FILE_PATHS); do
        tmux -L "${nested_tmux_server_socket_file_path:t}" kill-server 2>/dev/null
        rm -f "$nested_tmux_server_socket_file_path"
    done
    tmux -L default kill-server
}

alias TLV="tmux show-window-options"

#  %% -------- [AI] ----------:

function TMUX_KIRO_FORK_NEW_WINDOW() {
    local orig_pane_id=$(tmux display-message -p '#{pane_id}')
    local orig_window_id=$(tmux display-message -p -t "$orig_pane_id" '#{window_id}')
    local kiro_saved_session_file_path="/tmp/kiro_fork_$(date +%Y_%m_%d-%H_%M_%S).json"
    local new_pane_id=$(tmux new-window -a -t "$orig_window_id" -c "$PWD" -P -F '#{pane_id}')
    tmux send-keys -t "$orig_pane_id" "/chat save $kiro_saved_session_file_path --force" Enter
    tmux send-keys -t "$new_pane_id" "KK '/chat load $kiro_saved_session_file_path'" Enter
}

function KKE() {
    local original_dir="$PWD"
    cd ~/empty && kiro-cli chat --trust-all-tools
    cd "$original_dir"
}

function KKER() {
    local original_dir="$PWD"
    cd ~/empty && kiro-cli chat --trust-all-tools --resume
    cd "$original_dir"
}

function CCR() {
    CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1 claude --continue --dangerously-skip-permissions || CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1 claude --dangerously-skip-permissions
}

# claude code
alias CC="CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1 claude --dangerously-skip-permissions"

# kiro
alias KK="kiro-cli chat --trust-all-tools"
alias KKR="kiro-cli chat --trust-all-tools --resume"

# ai
alias AI="CC"
alias AIR="CCR"

#  %% -------- [sleep management caff nocaff driving mode] ----------:

function CAFF() {
    if [[ "$OSTYPE" == "darwin"* ]]
    then
        caffeinate -dimsu -t 7200 &
        echo "Caffeinate started for 2h (PID: $!)"
    else
        systemd-inhibit --what=idle:sleep:handle-lid-switch --who="Manual" --why="User requested" --mode=block sleep 7200 &> /dev/null &
        echo "Sleep inhibited for 2h (PID: $!)"
    fi
}

function NOCAFF() {
    if [[ "$OSTYPE" == "darwin"* ]]
    then
        pkill caffeinate && echo "Killed caffeinate" || echo "No caffeinate process found"
    else
        pkill -f "systemd-inhibit.*who=Manual" && echo "Sleep inhibitor killed" || echo "No sleep inhibitor found"
    fi
}

alias LIDCAFF='sudo pmset -a disablesleep 1'
alias NOLIDCAFF='sudo pmset -a disablesleep 0'
# driving mode os x. Need:
# export BT_HEADSET=""
# export HOTSPOT_SSID=""
alias DM='LIDCAFF && blueutil --connect $BT_HEADSET && networksetup -setairportpower en0 on && networksetup -setairportnetwork en0 "$HOTSPOT_SSID" "$(security find-generic-password -a "$HOTSPOT_SSID" -w)"'
alias NODM='networksetup -removepreferredwirelessnetwork en0 "$HOTSPOT_SSID" && networksetup -setairportpower en0 off && sleep 1 && networksetup -setairportpower en0 on && NOLIDCAFF'

# %% -------- [docker] ----------:

function DOCK(){
    sudo docker run -it -v $HOME/doc/config/config:/home/ubuntu/doc/config/config $@ main
}

function DOCKCWD(){
    sudo docker run -it -v $(pwd):$(pwd) -v $HOME/doc/config/config:/home/ubuntu/doc/config/config -w $(pwd) $@ main
}

alias DB="docker build"
alias DR="docker run"
alias DC="docker container"
alias DCE="docker container exec"
alias DI="docker image"

#  %% -------- [amazon brazil ADA] ----------:

function BWC() {
    brazil ws create --root "$@"
    GIG "$1" 2>/dev/null
}

function BRCAP() {
    local logfile="/tmp/brazil_recursive_build_output_$(date +%Y_%m_%d-%H_%M_%S).log"
    { brazil ws clean && brazil-recursive-cmd --allPackages brazil-build; } 2>&1 | tee "$logfile"
    echo "\nbrazil ws clean && brazil-recursive-cmd --allPackages brazil-build log saved to: $logfile"
}

# brazil
alias BB='brazil-build'
alias BWU='brazil ws use -p'
alias BWR='brazil ws remove -p'
alias BBCR='brazil ws clean && brazil-build clean && brazil-build release'
alias BBC='brazil-build clean'
alias BBR='brazil-build release'
alias BBDEPLOY="brazil-build cdk deploy --require-approval never"
alias BWSYNC="brazil ws sync && brazil ws sync --md && brazil ws sync && brazil ws sync --md"
alias BWSHOW="brazil ws show"
alias MW="mwinit -o"
alias KI="kinit -f"
alias CL="card local --runtimePort 8082"
alias CRPULL="cr-pull -w"

# %% -------- [config management] ----------:

function FAAC() {
    local was_in_config=false
    [[ $PWD == ~/doc/config/config* ]] && was_in_config=true
    (
        cd ~/doc/config && \
        (git clone git@github.com:zaid-g/config.git config.new || git clone https://github.com/zaid-g/config.git config.new) && \
        rm -rf config && \
        mv config.new config && \
        bash config/apply_config.sh
    )
    [[ $was_in_config == true ]] && cd ~/doc/config/config
}

alias RS="source ~/.zshrc && cd ."
alias AC="bash ~/doc/config/config/apply_config.sh"

#  %% -------- [unix archlinux linux] ----------:

function GRIM(){
    if [ "$#" -eq 1 ]; then
        grim -g "$(slurp)" ~/Downloads/screenshot-"$1"-"$(date +'%Y-%m-%dT%H:%M:%S').png"
    else
        grim -g "$(slurp)" ~/Downloads/screenshot"$(date +'%Y-%m-%dT%H:%M:%S').png"
    fi
}

alias S="sudo"
alias PO="systemctl poweroff"
alias PMU="sudo pacman -Syu"
alias X="xdg-open"
alias VM="virt-manager"
alias DISCORD="snap run discord"
alias FF="firefox"
alias CHROME="google-chrome-stable --ozone-platform=wayland"

# %% ---------- [zsh auto-completion] ----------:

function BP_AUTOCOMPLETE() {
    local brain_parent_path
    brain_parent_path=$(FIND_BRAIN_PARENT_PATH 2>/dev/null) || return
    compadd -- "$brain_parent_path"/*(N:t)
}

autoload -U compinit
zmodload zsh/complist
compinit
compdef BP_AUTOCOMPLETE VBP
# Include hidden files
_comp_options+=(globdots)
zstyle ':completion:*' menu select
bindkey -M menuselect '^[[Z' reverse-menu-complete

# 0 -- vanilla completion (abc => abc)
# 1 -- smart case completion (abc => Abc)
# 2 -- word flex completion (abc => A-big-Car)
# 3 -- full flex completion (abc => ABraCadabra)
zstyle ':completion:*' matcher-list '' \
    'm:{a-z\-}={A-Z\_}' \
    'r:[^[:alpha:]]||[[:alpha:]]=** r:|=* m:{a-z\-}={A-Z\_}' \
    'r:|?=** m:{a-z\-}={A-Z\_}'

# %% ---------- [colors and prompt display color COLOR_THEME echo print] ----------:

function PRINT_AS_HEADER() { printf '\033[1;33m%s\033[0m\n\033[1;33m%s\033[0m\n\033[1;33m%s\033[0m\n' "============================================================" "$1" "============================================================"; }

function COLORS(){
    local i
    for i in {0..255}; do
        printf "\x1b[38;5;${i}m██ %-5i\x1b[0m" $i
        (( (i+1) % 8 == 0 )) && echo
    done
}

export PS1='%F{$COLOR_THEME}%~ ▶%f '
export zle_highlight=("paste:bg=${COLOR_HIGHLIGHT_TEXT_ZSH},fg=${COLOR_TEXT_IF_COLORED_BACKGROUND}" "region:bg=${COLOR_HIGHLIGHT_TEXT_ZSH},fg=${COLOR_TEXT_IF_COLORED_BACKGROUND}")
export COLOR_THEME=28
export COLOR_TEXT_IF_COLORED_BACKGROUND=0
export COLOR_HIGHLIGHT_TEXT_ZSH=168
export COLOR_HIGHLIGHT_TEXT_TMUX=25
export COLOR_ACTIVE=196
export COLOR_HEX_ACTIVE_BACKGROUND=350000

# %% -------- [others] ----------:

# xdg open (open file from terminal with default app)
alias H="history 0"
# Bind Ctrl-space to clear-screen
bindkey '^@' clear-screen
alias DU="du -sh"
alias PING="ping google.com"
