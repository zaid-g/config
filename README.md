config files and install scripts

# ssh
git clone git@github.com:zaid-g/config.git
# https
git clone https://github.com/zaid-g/config.git


# %% -------- [firefox] ----------:

### treestyle tabs
import the json file from firefox -> extensions -> manage TST -> scroll all way to bottom -> import.
also you need to have tst-indent line installed.
